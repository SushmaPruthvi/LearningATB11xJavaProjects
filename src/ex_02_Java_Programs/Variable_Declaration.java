package ex_02_Java_Programs;

public class Variable_Declaration {

    public static void main(String[] args) {
        int _1 = 10;
        int $34 = 46;
        // we can declare the variables like this because the "identifiers" can accept the "_" and "$"

       // int enum = 10;
        // here it will give error because the "enum" is the keyword

        int Enum = 30;
    }
}
